# Master Plan

## Stage 0 — Project Discovery, Baseline and Automation Foundation
Status: Complete

Objectives:
- Record the actual infrastructure baseline, host roles, services, versions, ports, dependencies, and unknowns.
- Establish Project Brain governance, configuration/secrets policy, repeatable verification tools, and atomic-task automation workflow.
- Define and verify Stage 0 completion criteria.

Quality gates:
- Every infrastructure claim is classified and evidence-backed when marked `verified`.
- No secrets or unreviewed raw evidence are versioned.
- Verification is command-based, repeatable, readable, and returns meaningful exit codes.
- `NEXT_TASK.md` contains only one atomic, testable task.

### Completion Review — 2026-08-08
Outcome: `ready_for_stage_transition_approval`

The transition gate was explicitly approved on 2026-08-08.

Verified evidence covers the infrastructure baseline, SSH automation, service placement categories, critical listeners, Dify runtime reachability, PostgreSQL/Redis active-connection evidence, verification tooling, Project Brain governance, and the accepted n8n placement. Two `rdapp` containers, Qdrant reported version, Redis unauthenticated readiness, and sampled Qdrant active-use evidence remain known limitations; none blocks starting non-production product implementation. Stage 1 remains outside scope until the transition gate is explicitly approved.

## Stage 1 — Product Implementation
Status: In progress — local runtime and ST1-001 implementation baseline verified.

Objective: deliver the smallest safe, demonstrable vertical slice of the approved product direction without real organizational data or production deployment.

ST1-001 through ST1-005 verified the local synthetic intake, validation, canonicalization, duplicate gate, and deterministic credibility dispositions. ST1-006 added isolated durable PostgreSQL persistence, ST1-007 added controlled certification plus an append-only audit trail, ST1-008 added an idempotent Certified Knowledge projection restricted to certified records, ST1-009 added deterministic retrieval with source/certification provenance, and ST1-010/ST1-011 verified the first private Dify/Qdrant grounded-answer vertical slice. DEC-014 authorizes one bounded real business pilot; ST1-013 must first verify only the selected read-only file-share folder and its metadata constraints.

ST1-013 verified read-only pilot-folder access and a partial metadata inventory. The next gate is intentionally narrow: select a bounded initial document subset and explicit supported-format/extraction allowlist before any real file content is read or ingested.

ST1-045 completed controlled certification of seven explicitly approved historical management-report observations. It preserved the source-scope conflict and excluded all nonapproved items. ST1-046 then selected and read only one bounded, later internally dated management-report family under standing authorization. It produced seven runtime-local, provenance-backed Human Review candidates. The next gate is explicit Human Review; no candidate is certification-eligible until its exact disposition is supplied.

ST1-047 completed controlled certification and Certified Knowledge projection for all seven explicitly approved observations from the coherent bi-weekly reporting period `1402/11/21–1402/12/05`. The timeline advances only to that historical reporting period; present-day status remains insufficient. Index/RAG completion is blocked by the unchanged embedding invocation timing out with no Qdrant write. The next atomic task is bounded existing-provider runtime recovery; it may not change credentials, provider, model, vector schema, or threshold without separate approval.

ST1-048 completed the permitted existing-runtime diagnosis. It observed an embedding-related authentication failure alongside the controlled timeout but did not inspect or change the credential. The remaining work is approval-gated credential recovery or, separately, an approved provider/model decision; no further blind retry is planned.

ST1-049 performed the approved read-only diagnosis before any credential mutation. The existing embedding credential is recognized by Dify: one controlled synthetic embedding invocation succeeded at vector dimension 3072, while a separate generation credential/model also succeeded. Credential refresh is not required; the next task is controlled idempotent ST1-047 indexing and period-bound RAG verification without changing configuration.

ST1-050 verified the first end-to-end retrieval path for the approved bi-weekly management observations: the collection holds exactly seven new ST1-047 points without removing the 42 prior points, and a narrow source/period/metric-bound query returns provenance-backed historical framing at threshold `0.70`. Broad management retrieval correctly remains conservative. The next task returns to metadata-only currentness discovery for coherent status sources newer than `1402/12/05`.

ST1-051 exhausted the completed local metadata index for post-`1402/12` candidate families with coherent status semantics. It found no name/directory signals sufficient for safe automatic content selection. The next gate requests the normal business filing location for the latest periodic progress, dashboard, schedule, or status source; no broad crawl or speculative corpus selection is authorized.

ST1-052 completed the one approved business-locator recovery pass using only the same local metadata index. It found no deterministic project-wide source that can extend the verified period beyond `1402/12/05`; apparently later package-specific folders cannot establish report date, authority, or CEO-status semantics from metadata. The critical path is now a source-system owner or authoritative reporting location, not another discovery crawl.

ST1-053 acknowledges that source gap and leaves it blocked pending business input. In parallel it creates the decision-gated SDAS v0.1 proposal: an additive assurance model for Source through downstream consumption provenance, with explicit gaps for the current 49-item pilot. It makes no currentness, authority, certification, or runtime change. The next gate is architecture/governance approval before any additive assurance implementation.

ST1-055 implemented the approved SDAS v0.1 internal pilot with additive immutable assurance/consumption evidence tables. It back-assessed all 49 Certified Knowledge items only from persisted evidence, yielding `SDAS-1` / `assessed_partial` for all and zero reliance-eligible records. The next SDAS change is a decision gate; no v0.2 supersession/revocation, authority/currentness, reliance eligibility, or external rollout is implied.

ST1-056 through ST1-058 implemented and verified the approved private SDAS v0.2 provenance/policy pilot. One native synthetic record traversed source acquisition, deterministic transformation, policy automatic preparation, explicit human approval, controlled certification, registration, Certified Knowledge projection, isolated indexing, and provenance-backed retrieval. The 49 historical Certified Knowledge items remain `human_required` in the v0.2 simulation; no historical evidence was upgraded. Policy automatic approval does not certify a record, and currentness/authority/reliance remain out of scope.

ST1-014 confirmed that multiple bounded status-reporting subsets satisfy the technical size target. A business selection is required before content reading, because filesystem metadata does not establish which status period is authoritative.

DEC-015 selected `status_candidate_b`; bounded read-only extraction then completed for 18 of 19 documents without platform persistence or AI use. One selected XLSX failed deterministic OOXML extraction (`BadZipFile`), so the real-data corpus is partial and requires a human resolution/review gate before any real record can advance toward certification.

ST1-015 prepared the three successful real candidates for human decision without persisting real data. The XLSX remains unresolved because the approved SMB source was not reachable in the current session; `BadZipFile` is insufficient to infer corruption. The next critical-path gate is human review of the prepared candidates.

ST1-016 recorded `NEEDS_MORE_EVIDENCE` for all three first-pass candidates and confirmed that existing extracted text from the 18 approved PDFs is inadequate for the CEO project-status use case. The practical next step is bounded local OCR only after the exact selected-subset relative locator is recovered; broad share rediscovery is explicitly excluded.

ST1-017 recovered the exact selected subset through operator-provided bounded roots, validated the 19-entry signature, and completed local Persian OCR on all 18 PDFs. OCR did not yield report dates, physical progress, schedule, delay, risk, action, management decision, or status evidence; only two undated financial observations remain. This selected subset cannot answer the CEO project-status question. Selecting any new source requires a new explicit bounded business decision.

ST1-018 performed business-question-driven metadata discovery. Three materially different bounded series require user selection before content access: two planning-oriented series and one explicit project-status spreadsheet series. No new source content was read.

ST1-019 records the user’s selection of `status_oriented_candidate_1` as a bounded, read-only source only. Local deterministic extraction and review preparation may proceed strictly within its 18-file signature; the selection itself establishes neither source authority nor latest-status semantics.

ST1-020 recorded a complete Human Review with zero approvals. ST1-021 then performed a single bounded enrichment pass over the four unresolved sources. It found a visible Change Log row/status snapshot but no populated update date or authority/currentness evidence. The selected corpus is insufficient; the next critical path requires a specifically selected dated, authoritative project-status source rather than further reprocessing.

ST1-022 selected and extracted an internally dated daily-status workbook series. The source provides a time-contextualized historical activity snapshot and reviewable row provenance, but its authority and currentness beyond the greatest extracted reporting period remain unverified. ST1-023 is the mandatory Human Review gate; no real claim may advance without an explicit reviewer decision.

ST1-036/ST1-037 exhausted two later metadata-selected boundaries without a trustworthy current-status result. ST1-038 recorded `NEEDS_MORE_EVIDENCE` for all 15 associated candidates; none may enter the certification path.

ST1-039 is complete with a source gap. The bounded local metadata index and targeted document-control inspection did not identify the underlying authoritative periodic progress source required for the CEO-status question. The next critical path is a single business locator for the normal project-team filing location of the latest periodic progress, dashboard, schedule, or status report; it is not another broad discovery cycle.

The ST1-040 self-discovery override supersedes that human-locator gate. It ranked the completed runtime-local index and qualified one small family with source-attributed engineering/procurement evidence later than the existing certified timeline. The family does not establish a complete authoritative CEO-status snapshot; three Human Review candidates are the next gate, with no automatic certification or currentness change.

ST1-041 certified those three explicitly approved observations with strict source-attributed semantics. They extend the evidence timeline only in their narrow contexts; retrieval remains conservative at the unchanged threshold and current status remains insufficient. The critical path is targeted linkage/supersession discovery from those observations toward an authoritative overall status snapshot.

ST1-043 executed one user-directed metadata-only locator search after no business filing locator was available. A bounded management-report candidate has the strongest name/hierarchy signals, but its content, authority, reporting period, and currentness remain unknown. The next gate is a single business confirmation before any separate bounded content-access task; existing ST1-042 Human Review candidates remain untouched.

ST1-044 completed bounded local extraction after that confirmation. The source contains a historical management-level snapshot with plan/actual, discipline/site, constraint, and financial-context signals, but its newest internal date signals precede the already certified `1402/06/25-1402/06/31` period. A title/scope/date conflict remains explicit. Ten provenance-backed candidates await Human Review; no trust, currentness, or certification policy changed.

ST1-071 parks the unresolved E1/E2/E3 governance dependency as `WAITING_FOR_EXTERNAL_EVIDENCE` and continues SDAS development without bypassing that gate. The selected independent milestone is the Assurance Verification and Evidence Passport layer: an additive read model on `rddb` plus a private loopback route on `rdapp` that explains a certified datum's assurance chain from append-only evidence only. Synthetic verification now distinguishes `VERIFIED`, `VERIFIED_WITH_LIMITATIONS`, `HUMAN_REQUIRED`, `NOT_RELIANCE_ELIGIBLE`, `REVOKED_OR_SUPERSEDED`, and `QUARANTINED` without weakening certification, currentness, authority, or reliance boundaries.

ST1-072 extends that layer into a portfolio operating view. The additive summary and exception-queue projections now let operators see deterministic passport counts and only the non-`VERIFIED` items that require triage. The live private runtime still shows 50 real items in `HUMAN_REQUIRED`, which confirms the unresolved governance dependency is the dominant real blocker, not a missing technical queue layer. The next independent step should move closer to the original ST1-066 operating model by summarizing SDAS routing at the record/policy-decision stage before certification.

ST1-073 completes that next operating step. The private runtime now exposes deterministic pre-certification routing counts and a non-`policy_automatic` exception queue over immutable record, policy, assurance, source, and delegation evidence. Synthetic verification proves that only an exact-scope synthetic active delegation can surface `policy_automatic`; unmatched governance remains `WAITING_FOR_EXTERNAL_EVIDENCE`. The live private runtime currently reports `policy_automatic=0`, `human_required=61`, and `reject_or_quarantine=2`, confirming the blocker is still external governance evidence rather than missing routing logic. The next independent milestone should move from portfolio counts to single-record operator explainability over that routing layer.

ST1-074 completes that per-record explainability step. Operators can now inspect one record through a private detail route and see exactly which policy, assurance, source, and governance signals produced the normalized routing outcome. A live real queue item still resolves to `human_required` with `WAITING_FOR_EXTERNAL_EVIDENCE`, so the system now exposes the blocker precisely instead of only counting it. The next step should stop polishing generic routing views and move directly toward the real ST1-066 target: selecting one already-authorized recurring LOW-risk real class and calculating the smallest truthful governance gap that prevents its first real `policy_automatic` hard gate.

ST1-075 completes that selection. The chosen class is the recurring Project Controls progress workbook family represented today by `enterprise_ai_real_action_plan_weekly_observation`. It is the strongest in-scope real class because its workbook schema, reporting-week semantics, and LOW-risk fact boundaries are already evidenced. The blocker is not merely "authority missing": E1 and E2 remain missing, E3 remains partial, no real source/system for this class is registered in the runtime, and no native acquisition chain exists yet for this class. The next step should convert that selection into a candidate-specific governance/source-registration bundle so real business evidence can be applied without reinterpreting scope.

ST1-076 completes that bundle. The candidate-specific scope, permitted/prohibited facts, reporting-time rule, and required A1/A2/A3 + source-registration inputs are now frozen in one reusable artifact. No further generic SDAS routing abstraction is needed before the next step. Progress toward the first real `policy_automatic` now depends on real controlled organizational evidence for this exact class, not on another internal discovery cycle.

ST1-077 reduces that external gate one step further. The selected class now has a plain-Persian, business-facing evidence request pack so the organization can provide exactly the needed controlled confirmations without translating SDAS fields. The remaining path is no longer a discovery or design problem; it is the receipt and validation of real A1/A2/A3 plus source-registration evidence for this exact workbook class.

ST1-078 adds the deterministic local intake validator for that exact evidence bundle. The next real gate still depends on external controlled evidence, but once it arrives the repository no longer needs ad-hoc interpretation of bundle shape, fact boundaries, or reporting-period rules. The remaining blocker is therefore the evidence itself, not missing local validation machinery.

The ST1-078 handoff is now operationally complete on the repository side: there is a business-facing Persian request pack, a machine-checkable intake spec, a strict validator, and a submission template with explicit `REQUIRED_INPUT` placeholders. The next truthful move toward the first real `policy_automatic` hard gate is receipt of real controlled evidence for that exact workbook class.

ST1-078 now also has a local readiness assessor, so receipt of a filled bundle no longer yields only a binary validation result. The repository can deterministically distinguish missing business evidence, scope/policy violations, and structurally acceptable-but-unverified submissions before any activation or native acquisition step.

ST1-079 formally parks that external dependency and moves the critical path back to independent SDAS assurance work. The repository-side passport contract is now versioned as `SDAS Assurance Passport v0.1`, with explicit dimension-level explanation and a distinct `INTEGRITY_FAILURE` outcome for deterministic chain/provenance breaks. The next meaningful milestone is no longer more ST1-078 tooling; it is deployment and runtime verification of the upgraded passport/views on the private ingestion stack.

ST1-080 completes that runtime step. The upgraded passport/views are now deployed on the private stack and verified against deterministic synthetic cases, including the new `INTEGRITY_FAILURE` path. The external governance blocker remains unchanged, so the next independent SDAS milestone should avoid governance restatement and instead address a still-partial adjacent capability, such as vector/index projection visibility in the assurance surface.

ST1-081 completes that adjacent visibility gap. Operators can now inspect, for an already certified datum only, whether an isolated Qdrant projection exists and whether its stored payload identity matches the Certified Knowledge identity, without exposing raw vectors, provider state, or uncertified records. This does not move the real governance blocker, but it removes another observability blind spot around the certified path. The next task should return directly to the ST1-066 hard gate by quantifying what the selected recurring workbook class would do under exact-scope activation conditions, without activating any real delegation or certifying anything automatically.

ST1-082 completes that forecast. The answer is now evidence-backed: exact-scope governance/source-control activation alone would unlock zero current records from the selected recurring workbook class, because all ten currently known rows are historical/reconstructed simulation records with persisted reason `missing_native_evidence`. This means the next critical-path change is no longer another routing forecast. The project now needs a deterministic first-native-record preflight for this selected class so that, once the external evidence bundle truly arrives, source registration, native acquisition, business-time resolution, and first policy evaluation can proceed without improvisation.

ST1-083 completes that preflight. The project now has a deterministic local-only verifier that separates bundle readiness from first-native-record readiness and proves four meaningful states, including a synthetic `READY_FOR_FIRST_REAL_RUNTIME_ATTEMPT`. This still does not create any real source, acquisition, or delegation state, but it means the next most valuable step is no longer another validator. The next step should prepare the exact dry-run runtime transaction plan for the first real attempt, so that when the external evidence and first native metadata artifact arrive, the system can execute the smallest truthful source-registration/acquisition/policy path without guesswork.

ST1-084 completes that dry-run plan. The first real selected-class attempt is now decomposed into an ordered six-write sequence with preserved hard stops, and the planner already distinguishes blocked vs ready inputs without mutating runtime state. This means the next additive step should stop refining planning logic and instead compile those approved inputs into one non-secret execution manifest shape, so that the eventual real attempt can move from "planned" to "operator-executable" with minimal translation risk.

ST1-085 completes that manifest compilation step. The project now has a non-secret execution-manifest shape that directly maps verified business evidence plus native-record metadata into the exact six-step runtime attempt, while still preserving all hard stops and avoiding secrets. The next remaining additive gap before a real attempt is a concise operator handoff/checklist layer: one deterministic checklist for what the operator must confirm and what evidence artifacts must exist immediately before the first real run.

ST1-086 completes that handoff layer. The project now has a concise operator-facing readiness artifact that says, from the existing preflight/plan/manifest stack, whether a real run may begin and exactly what must still be supplied or verified. The next additive step should not add another planning layer. It should package these existing artifacts into one submission-ready operator kit so the moment real evidence arrives, the first runtime attempt can be executed with minimal coordination overhead.

ST1-087 completes that packaging layer. The repository now has one deterministic operator kit that assembles readiness, ordered runtime actions, required operator inputs, and preserved hard stops into a single non-secret artifact set. The next additive gap is no longer packaging. It is the deterministic independent-verification gate that must sit immediately before the first real runtime mutation, so incoming external evidence and native-record metadata can be checked against exact-scope requirements without improvisation.

ST1-088 completes that independent-verification gate. The project can now determine, without mutation, whether a future real evidence submission is actually ready to begin the six-step runtime write sequence and exactly why it is blocked if not. The next additive step should move to the opposite side of that boundary: a deterministic post-mutation receipt/hard-stop verifier that can confirm the first real runtime attempt truthfully reached `policy_automatic` and then stop before certification.

ST1-089 completes that first post-mutation hard-stop verifier. The project can now prove, with a deterministic receipt contract, whether the first real selected-class runtime attempt actually reached `policy_automatic` and whether the certification boundary remained intact. The next additive step should move to the ST1-066 operating-model requirement: deterministic policy simulation over the selected-class queue so future routine batches can report X `policy_automatic`, Y `human_required`, and Z `reject_or_quarantine` without record-by-record review.

ST1-090 completes that operating-model simulation step. The project now has a deterministic batch-level routing surface for the selected class, including exception-focused Human Review handling and false-positive safety checks that preserve the no-native-upgrade rule. The next additive step should transform that simulation into an operator-facing exception queue/review-pack contract so only `human_required` and `reject_or_quarantine` items are prepared for review when the first real native flow becomes available.

ST1-091 completes that exception-focused review surface. The project can now turn selected-class batch routing into a deterministic operator review pack that excludes `policy_automatic` items from individual review and carries forward only the exact exceptions. The next additive step should compile the explicit ST1-066 hard-stop report contract so that, once the first real native record reaches `policy_automatic`, the system can emit the required pre-certification report fields without improvisation.

ST1-092 completes that explicit hard-stop reporting layer. The project can now emit the exact non-secret pre-certification report contract required by ST1-066 section D as soon as a truthful `policy_automatic` result exists. The next additive step should package the full first-real-attempt evidence chain into one operator-facing execution dossier so that, when external evidence finally arrives, the first real native run can proceed with minimal coordination overhead and a ready hard-stop report surface.

ST1-093 completes that operator-facing dossier layer. The project now has one deterministic non-secret dossier that packages the selected-class first-real execution path from pre-mutation readiness through hard-stop reporting and exception handling. The next additive step should focus on the remaining external dependency edge: a deterministic intake-to-dossier handoff surface for independently verified external evidence, so that when real business evidence arrives the dossier can be populated and the first truthful native attempt can actually be run.

ST1-094 completes that handoff surface. The project can now translate independently verified selected-class external evidence into dossier-ready shapes without ad-hoc operator reinterpretation. The next additive step should compile a final operator launch package that combines this handoff surface with the ready dossier so the first truthful native run can begin immediately when real verified evidence arrives.

ST1-095 completes that final local launch-package layer. The repository now has a deterministic non-secret operator package that can be populated immediately when real verified evidence arrives, without further local assembly work. The next additive step should be the smallest remaining operator-facing artifact still missing before the real gate itself: a concise real-run blocking/status summary that can classify `ready_to_run`, `waiting_for_external_evidence`, or `waiting_for_runtime_only_fields` from the same launch package inputs.

ST1-096 completes that operator-facing readiness summary. The repository can now classify the selected-class first real attempt into the exact practical run states an operator needs without inspecting the entire chain manually. The next additive step should stop expanding internal packaging surfaces and instead compile one final business-facing missing-input pack that names only the real-world evidence or runtime-only values still required when the status is not `ready_to_run`.

ST1-097 completes that missing-input narrowing step. The repository now exposes only the exact remaining external-evidence or runtime-only requirements for each non-ready state instead of a broad blocker chain. ST1-098 then adds a deterministic reentry gate on top of the parked dependency fingerprint and readiness stack, so unchanged external state stays parked while genuine dependency or readiness changes reopen the first-real path. The next additive step should be one single-command rehearsal runner that executes this existing local stack end-to-end and returns the truthful selected-class first-real status without manual script stitching.

ST1-099 completes that orchestration step. The selected-class local stack can now be exercised in one deterministic command that returns the truthful parked/reopen/runtime-only/ready status and immediate next action without mutating runtime state. The next additive step should compare any future externally supplied bundle/native artifact against the parked baseline so only genuine input deltas reopen the first-real path.

ST1-100 completes that delta-comparison step. The repository can now prove whether a future bundle/native submission is unchanged or genuinely reentry-relevant, and it exposes the exact high-signal deltas that move the selected-class path from parked toward ready. The next additive step should summarize that low-level comparator output into one concise business-facing change-impact surface for future external submissions.

ST1-101 completes that summary step. The repository now has a concise exact review surface that translates low-level submission deltas into business-facing readiness and next-action transitions without weakening any trust boundary. The next additive step should turn this into a candidate-submission checklist so future real evidence arrivals can be checked against the exact remaining files, fields, and runtime artifacts before first-real execution.

ST1-102 completes that checklist step. The repository can now express future selected-class real submissions as an exact pre-run checklist with either one remaining native-evidence requirement or a zero-item ready state. The next additive step should package a future real submission into one deterministic arrival packet so the first-real local stack can consume one operator-ready payload instead of separate files.

ST1-103 completes that arrival-packet step. The repository can now represent a future selected-class real submission as one exact operator-ready payload that already includes readiness impact, next action, changed facts, and remaining checklist items. The next additive step should compile a first-real pre-mutation execution envelope so that packet can be handed directly into the existing hard-stop and execution path without further manual assembly.

ST1-104 completes that execution-envelope step. The repository can now represent a future selected-class ready submission as one exact pre-mutation execution object aligned to the existing first-runtime path, including ordered runtime steps, hard-stop readiness, and preserved required operator inputs. The next additive step should reduce this object to the exact mutation-start handoff and before-step-one confirmations only.

ST1-105 completes that mutation-start reduction step. The repository can now represent a future selected-class ready submission as the exact mutation-start payload plus before-step-one confirmations only, without weakening any trust boundary. The next additive step should compile a first-step launch card that isolates the exact initial write target and preserved hard stops at the moment a real run starts.

ST1-106 completes that first-step isolation step. The repository can now reduce a future selected-class ready submission to the exact initial write target `sdas_source_registry`, the confirmations that must still hold, and the preserved hard stops at the moment the real run starts. The next additive step should reduce this first step further into a source-registration step card with only the minimal field-level payload and confirmations for that initial write.

ST1-107 completes that source-registration narrowing step. The repository can now reduce the first write target `sdas_source_registry` to its minimal field-level payload and required confirmations only. The next additive step should apply the same narrowing to the second write target, `sdas_source_control_verifications`, so the early runtime path remains equally exact and operator-ready.

ST1-108 completes that second-write narrowing step. The repository can now reduce the source-control verification target `sdas_source_control_verifications` to its minimal field-level payload and required confirmations only, including deterministic normalization of the approved business-time rule and explicit preservation of runtime-required verified-bundle fields. The next additive step should apply the same narrowing to the third write target, `sdas_acquisition_events`, so the first real path continues as one exact operator-ready sequence without broad payload surfaces.

ST1-109 completes that third-write narrowing step. The repository can now reduce the acquisition target `sdas_acquisition_events` to its minimal field-level payload and required confirmations only, while preserving exact acquisition continuity fields and explicit runtime-derived placeholders where the real run must still supply verified values. The next additive step should apply the same narrowing to the fourth write target, `sdas_transformations`, so transformation continuity enters the path with the same exactness.

ST1-110 completes that fourth-write narrowing step. The repository can now reduce the transformation target `sdas_transformations` to its minimal field-level payload and required confirmations only, preserving deterministic lineage-continuity fields without exposing broader execution surfaces. The next additive step should apply the same narrowing to the fifth write target so the real intake request enters the path with the same exactness.

ST1-111 completes that fifth-write narrowing step. The repository can now reduce the record-intake target `POST /v1/records -> ingestion.credibility_records` to its minimal field-level payload and required confirmations only, including the exact low-risk payload shape and provenance anchors that must be supplied at runtime. The next additive step should apply the same narrowing to the sixth write target so the first real `policy_automatic` decision surface itself is isolated.

ST1-112 completes that sixth-write narrowing step. The repository can now reduce the policy-decision target `sdas_policy_decisions` to its minimal field-level payload and required confirmations only. All six planned runtime writes are now individually narrowed into deterministic step cards. The next additive step should compile one exact pre-certification hard-stop gate package that assembles these six surfaces into the smallest truthful operator handoff before the first real `policy_automatic` attempt.

ST1-113 completes that hard-stop assembly step. The repository can now assemble all six narrowed runtime writes, the preserved hard-stop report, and the final operator launch surface into one truthful pre-certification gate package. The next additive step should collapse this package further into a single runtime-only submission card that shows only the exact five operator-supplied values still needed to execute the first real attempt under the unchanged hard stops.

ST1-114 completes that runtime-only collapse step. The repository can now reduce the selected-class first-real path to the exact five real operator-supplied values still required at execution time, while preserving the unchanged hard stops and certification boundary. The next meaningful step should either execute the first real selected-class attempt when independently verified real evidence is available, or compile one exact execution worksheet that maps those five values to the existing runtime sequence without adding new trust semantics.

ST1-115 completes that execution-mapping step. The repository can now map the exact five real runtime values back onto the six-step runtime sequence and preserved stop-before-certification boundary with no ambiguity. The next meaningful step should collapse this again into one deterministic trigger card that answers only: execute now, wait for external evidence, or wait for remaining runtime values.

ST1-116 completes that trigger collapse step. The repository can now resolve the selected-class first-real path to one truthful next action only: `execute_now`, `wait_for_external_evidence`, or `wait_for_runtime_only_values`. The next meaningful step should produce the smallest activation-request packet needed to move that trigger toward `execute_now` using real independently verified evidence, rather than adding another internal packaging layer.

ST1-117 completes that activation-request step. The repository can now express the smallest truthful request needed to move the selected-class first-real path toward `execute_now`. The next meaningful step should produce one final pre-execution operator brief that packages the exact request, the six-step mapping, and the certification hard stop into a single handoff fit for the first real attempt.

ST1-118 completes that final local handoff step. The repository can now express the selected-class first-real path as one concise pre-execution operator brief that combines the truthful next action, the smallest remaining request, the six-step runtime mapping, and the preserved pre-certification hard stop. The next meaningful step should not add another packaging layer. It should verify future real execution conformance: whether an actual first-real runtime receipt and observed step sequence match this brief exactly and still stop before certification.

ST1-119 completes that final independent local verification step. The repository can now compare a future runtime receipt against an approved pre-execution brief artifact and detect exact conformance failures, including approval-mode drift and certification-boundary breaches. At this point the critical path is no longer another local assembler or verifier. The next meaningful milestone is the first real selected-class runtime attempt itself, which remains gated only by new independently verified external governance/source-control evidence plus one native selected-class record artifact.

ST1-120 remains blocked as of Tuesday, August 11, 2026, and that blocker is now re-proven from current authoritative state rather than assumed. The repository workspace still contains no real independently verified arrival bundle and no real native selected-class artifact, while runtime still has zero selected-class source registration, acquisition, transformation, and active delegation rows. No smaller tooling step remains on the critical path without restating the blocker. The next meaningful milestone is unchanged: the first real selected-class runtime attempt on arrival of those two real inputs.

ST1-121 narrows that blocker truthfully after the later business decision approving a limited pilot-governance boundary. No historical/source authority was upgraded, no real delegation became active, and no certification occurred. However, one already-authorized real ST1-061 workbook is now re-verified from runtime state as having already traversed source registration, read-only native acquisition, deterministic transformation, record intake, and policy evaluation, where it truthfully remains `human_required` because authority and business reporting time are still unresolved. The critical path therefore shifts from a generic “external evidence deadlock” to one narrower evidence ask: obtain exactly one independent source-authority or reporting-period artifact for that same source/class so the existing real native record can be re-evaluated without widening scope or weakening trust boundaries.

ST1-122 further refines that path to match the active ST1-066 success criterion. ST1-061 is now explicitly demoted to a regression/reference case and must remain `human_required`; it is not the first real `policy_automatic` success target. The selected recurring class remains the same, but the representative success-target candidate is now the already-authorized recurring workbook with explicit document-content reporting period `1402/11/21–1402/12/05` inside the selected Maroon Project Controls workbook family. The blocker is therefore narrowed again: the smallest remaining real-world ask is a reusable class-scoped governance/report-definition bundle (`A1`, `A2`, `A3`, plus stable source-registration identity) for that recurring workbook series, not a record-specific rescue of ST1-061.

ST1-123 turns that narrowed blocker into an exact business handoff. The repository now contains a Persian request pack addressed to the selected recurring workbook series and asking for only the four real-world inputs that matter: `A1`, `A2`, `A3`, and one stable non-sensitive source-series identifier. This is the smallest truthful external ask now left on the critical path before the first real `policy_automatic` hard-stop attempt can begin for that series.

ST1-124 operationalizes that handoff into one exact machine-checkable intake kit for the same recurring workbook series. The repository now has a selected-series template and verifier that preserve the generic ST1-078 contract while freezing the exact success-target boundary: canonical source id `maroon_project_controls_progress_workbook_series`, representative workbook alias `source-a08f4a79cf2116b1`, representative filename `070-TWRP-24 1402-12-05.xlsx`, and representative document-content reporting period `1402/11/21–1402/12/05`. The blocker is no longer ambiguity about submission shape. It is only the remaining real inputs themselves: `A1`, `A2`, `A3`, stable source-registration evidence reference, and stable non-sensitive source-series identifier.

ST1-125 completes the last local-only intake ambiguity for that same selected series. The repository now has one deterministic gate runner plus one positive synthetic filled fixture proving both truthful selected-series outcomes: the template remains `WAITING_FOR_EXTERNAL_EVIDENCE`, while a filled exact-scope bundle reaches `PENDING_INDEPENDENT_VERIFICATION`. No real delegation, source registration, or certification occurs in either case. The remaining critical-path blocker is therefore only the arrival of one real sanitized filled bundle for the same recurring workbook series.

ST1-127 completes the last local-only post-gate ambiguity for that same selected series. The repository now has one deterministic independent-verification handoff that converts a future exact-scope filled bundle into the exact six controlled checks still required before any real selected-series runtime mutation can begin. No real delegation, source registration, native acquisition, or certification occurs. The critical path is now reduced to one real sanitized filled bundle for the selected recurring workbook series plus the controlled review of that bundle's exact scope, signer identity, source ownership, and business-time evidence.

ST1-129 adds the first direct evidence audit of the whole ST1-066 objective. The repository can now prove, section-by-section, that selection, simulation, operating model, and hard-stop preparation are in place, while the actual native real selected-series `policy_automatic` path remains unproven. The remaining critical path is now explicit and minimal: one real sanitized filled selected-series bundle, controlled review of that bundle, one real native selected-series record artifact, and one real `policy_automatic` hard stop without certification.

ST1-131 removes the last local-only ambiguity on the native-record side of that critical path. The repository can now prove not only class-level native-record readiness, but exact selected-series native-record readiness for the ST1-122 target itself. The remaining critical path is therefore unchanged in substance but narrower in ambiguity: one real sanitized filled selected-series bundle, one real selected-series native-record artifact, controlled review of the bundle, and one real `policy_automatic` hard stop without certification.

ST1-132 closes the last local-only ambiguity across those two inputs together. The repository can now prove that an exact-scope selected-series bundle plus an exact-scope selected-series native-record artifact are sufficient to advance the path to controlled review, with no further local interpretation layer required. The remaining critical path is now purely real-world: the arrival of those two real selected-series artifacts, their controlled review, and the first real `policy_automatic` hard stop without certification.

ST1-134 turns that remaining real-world blocker into one operational business handoff. The repository now contains one concise Persian request pack that asks for exactly the two real selected-series artifacts still needed and explicitly preserves the non-activation / non-certification boundary. The critical path is unchanged in substance, but the next handoff is now as narrow and unambiguous as the local tooling path itself.

## Later Stages
Future work may implement ingestion, validation, normalization, quality scoring, HITL, lineage, certified knowledge, RAG/AI services, automation, and observability. Scope, design, and readiness criteria for later stages must be approved explicitly; they are not established by this document.
